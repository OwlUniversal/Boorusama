name: Build and Release Android APK

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          # This will automatically pull the newest stable (e.g., 3.35.6 or higher)
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Make scripts executable
        run: chmod +x gen.sh build.sh

      - name: Run code generation
        # Adding the delete flag just in case there are generation conflicts
        run: ./gen.sh --delete-conflicting-outputs

      - name: Build Production APK
        run: ./build.sh apk --flavor prod

      - name: Create Release and Upload APK
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            build/app/outputs/flutter-apk/app-prod-release.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Artifact (for manual runs)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: Boorusama-Plus-APK
          path: build/app/outputs/flutter-apk/app-prod-release.apk
